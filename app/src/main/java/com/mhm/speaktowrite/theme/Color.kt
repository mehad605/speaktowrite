package com.mhm.speaktowrite.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────────────────────────────────────
// Speak to Write — Matte UI Color Palette (Zinc/Matte Redesign)
// ─────────────────────────────────────────────────────────────────────────────

// Base canvas: Zinc 950 and dark gray/black
val AuroraBackground = Color(0xFF09090B)       // Zinc 950
val AuroraBackgroundEnd = Color(0xFF121214)    // Dark gray/black phone canvas

// Surface tiers — Zinc 900 and solid borders
val AuroraSurface = Color(0xFF1E1E22)          // Zinc 900 variant
val AuroraSurfaceHigh = Color(0xFF27272A)      // Zinc 800
val AuroraSurfaceHighest = Color(0xFF3F3F46)   // Zinc 700
val AuroraBorder = Color(0xFF2C2C30)           // Solid border
val AuroraBorderStrong = Color(0xFF3F3F46)

// Matte brand colors
val Emerald = Color(0xFF10B981)               // Emerald 500
val EmeraldBright = Color(0xFF34D399)         // Emerald 400
val Teal = Color(0xFF06B6D4)                  // Cyan 500
val TealDeep = Color(0xFF0891B2)              // Cyan 600

// Text: Zinc 50, Zinc 400, Zinc 500
val TextPrimary = Color(0xFFFAFAFA)            // Zinc 50
val TextSecondary = Color(0xFFA1A1AA)          // Zinc 400
val TextTertiary = Color(0xFF71717A)           // Zinc 500

// Semantic colors
val AuroraSuccess = Color(0xFF10B981)
val AuroraWarning = Color(0xFFF59E0B)          // Amber 500
val AuroraError = Color(0xFFEF4444)            // Red 500
val AuroraInfo = Color(0xFF8B5CF6)             // Violet 500

// Legacy aliases
val SolidGreen = Emerald
val DarkBackground = AuroraBackground
val DarkSurface = AuroraSurface

